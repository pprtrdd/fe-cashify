class CategoryEntity {
  final String id;
  final String name;
  final bool isExpense;
  final bool isExtra;

  CategoryEntity({
    required this.id,
    required this.name,
    required this.isExpense,
    required this.isExtra,
  });
}
